# 英音发音辅助学习系统 — 开发文档 (v2)

> 本文档面向 Claude Code。目标是构建一个本地部署（并预留 Docker 打包能力）的 Web 应用，帮助学习者练习英式英语（DJ / Gimson 体系）的元音发音，通过共振峰（formant）声学分析给出可视化、可执行的反馈。

---

## 1. 项目概述

### 1.1 背景与教学原理

英语元音的听感区别，声学上主要来自前两个共振峰 F1 与 F2。F1 与舌位高低（开口度）相关，F2 与舌位前后相关。学习者发元音不准，本质是"声道共鸣腔没摆到目标形状"。本系统核心价值：**测出用户发音的 F1/F2，打在参考坐标图上，与标准英音元音的靶心对照，把抽象的舌位偏差变成可见、可纠正的反馈。**

标注体系固定为 **英式英语 RP（Received Pronunciation），DJ / Gimson 现代修订体系**（带长度符号 ː，i/ɪ、u/ʊ 用不同字母形状区分）。不涉及美音。

### 1.2 两张核心图（重要设计点）

系统保留**两张不同的元音图**，对应发音的两个层面，且视觉上要能相互类比：

1. **IPA 元音四边形（发音层面）**：上宽下窄的梯形。横轴 = 舌位前后，纵轴 = 舌位高低。教材经典图，作为"动作指导"。
2. **F1–F2 共振峰图（声学层面）**：横轴 = F2，纵轴 = F1。用户录音测得的点打在这张图上，是"结果测量"，也是打点对照功能的载体。

**关键实现要求 —— 坐标轴方向必须刻意反向，使两张图形状对应：**
- F1 轴（纵轴）**反向**：F1 数值向下增大（低 F1 = 高舌位元音如 /iː/ 落在**上方**）。
- F2 轴（横轴）**反向**：F2 数值向左增大（高 F2 = 前元音如 /iː/ 落在**左侧**）。
- 处理后 /iː/ 落左上、/ɑː/ 落右下、/uː/ 落右上，与 IPA 四边形一致。学习者可把 F1–F2 图上的偏移直接翻译回舌位动作（"点偏右 → 舌位太靠后 → 往前伸"）。**这是教学价值的关键，勿用常规正向坐标。**

可选联动：点击 F1–F2 图上的点，IPA 四边形上高亮对应舌位区域（阶段一可选）。

### 1.3 三类对照点

F1–F2 图上最终会同框显示三类信息，需用图例清晰区分：
- **文献靶心**：群体统计的标准值 + 可接受范围椭圆（代表"标准英音平均位置，标准是一片区域而非一个点"）。
- **示范音点**：真人标准发音录音测得的点（服务端预分析并缓存）。示范音就绪后出现。
- **用户多遍点**：用户本次录音自动分出的每一遍各一个点，叠加显示，并附集中趋势（中心）与散布（椭圆/标准差）。

---

## 2. 技术栈与环境

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | Python + FastAPI | 异步，便于阶段二加 WebSocket 实时分析 |
| 声学分析 | praat-parselmouth | Praat 核心算法绑定，共振峰/基频/强度/频谱，数值对标 Praat 桌面版 |
| 数值计算 | numpy | 端点检测、统计、稳态取值 |
| 文件上传 | python-multipart | 接收音频 |
| 环境管理 | **uv** | 虚拟环境 + 依赖 + Python 版本；uv.lock 保证可复现 |
| 前端 | React + Vite + TypeScript | 主流、生态大 |
| 录音 | Web Audio API + MediaRecorder | **浏览器端采集**，不经后端硬件 |
| 可视化 | Canvas 或轻量图表库 | 两张元音图、三类点、（阶段二）声谱图与轨迹 |
| 音频转码 | ffmpeg（系统依赖） | 浏览器 webm/opus → wav 供 parselmouth 读取 |

### 2.1 部署环境与约束

- 本地部署，运行在 **WSL（Linux）**；预留 Docker 打包能力（§9）。
- **关键约束：音频输入必须由浏览器负责，后端不直接访问麦克风。** WSL 对音频硬件访问不直接，桥接易出问题。架构固定为：浏览器采集 → 传后端 → parselmouth 分析。

### 2.2 后端环境（uv）

- `pyproject.toml` 声明依赖，`uv.lock` 锁版本。
- 依赖：`fastapi`、`uvicorn[standard]`、`praat-parselmouth`、`numpy`、`python-multipart`。
- 流程：`uv venv` → `uv add ...` → `uv run uvicorn app.main:app --reload`。
- **WSL 提示**：parselmouth 在 Linux 通常有预编译 wheel，`uv add` 一般直接装上；若需编译，先 `sudo apt install build-essential`。webm 转码需 `ffmpeg`（`sudo apt install ffmpeg`）。README 写明。

### 2.3 可容器化约束（从一开始遵守，便于后期打包）

即使阶段一不写 Dockerfile，也须遵守以下约束，使后期加 Docker 无需返工：
- **系统依赖清单化**：ffmpeg 等系统级依赖显式记录，勿依赖隐式环境。
- **配置外部化**：参考数据路径、共振峰频率上限、取段/分遍参数、CORS 来源等一律走环境变量或配置文件，不硬编码。
- **前端可被后端托管**：前端构建产物（dist）应能由 FastAPI 作为静态文件托管，使单容器即可运行整个应用。

---

## 3. 项目结构（建议）

```
english-vowel-trainer/
├── backend/
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── config.example.toml       # 外部化配置示例（参数见 §6.5）
│   └── app/
│       ├── main.py               # FastAPI 入口、路由、（打包时）静态托管
│       ├── analysis.py           # parselmouth 封装：共振峰、基频、强度、声谱图
│       ├── segmentation.py       # 端点检测(VAD) + 自动分遍 + 稳态段截取
│       ├── quality.py            # 逐遍质量控制、共振峰合理性检查
│       ├── reference.py          # 加载标准元音参考数据 + 示范音预分析缓存
│       ├── models.py             # Pydantic 请求/响应模型
│       ├── config.py             # 读取外部化配置
│       └── data/
│           ├── vowels_rp.json    # 12 单元音参考共振峰（见 §5）
│           └── demo_audio/       # 示范音（后补）
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── pages/
│       │   ├── TheoryPage.tsx
│       │   ├── VowelChartPage.tsx
│       │   └── PracticePage.tsx
│       ├── components/
│       │   ├── IpaQuadrilateral.tsx
│       │   ├── FormantChart.tsx       # 三类点 + 反向坐标
│       │   ├── Recorder.tsx
│       │   ├── MultiTakePanel.tsx     # 多遍结果列表 + 统计
│       │   └── SourceFilterFigure.tsx
│       ├── api/client.ts
│       └── data/vowels.ts
├── docker/                        # 后期填充（§9 预留规格）
│   ├── Dockerfile                 # 预留
│   └── docker-compose.yml         # 预留
└── README.md
```

---

## 4. 功能范围与分期

### 阶段一（第一版，纯元音，REST 架构）— 必做

1. **理论介绍页**：可视化讲解声源-滤波器原理（§7）。
2. **IPA 元音四边形**：12 单元音标准舌位。
3. **F1–F2 共振峰图**：反向坐标（§1.2），标出 12 元音靶心 + 可接受范围椭圆。
4. **标准示范播放**：播放标准元音音频（示范音后补，§5.3；缺失时优雅占位）。
5. **示范音共振峰分析**：服务端对示范音**预分析并缓存** F1/F2，作为"示范音点"显示（示范音就绪后）。
6. **录音**：浏览器端录制，可含多遍。
7. **★ 自动分遍分析（核心）**：一段录音经 VAD 自动切成每一遍 → 每遍内部取稳态段 → 各出一个 F1/F2 点 → 逐遍质量控制 → 返回多段数组 + 整组汇总统计。
8. **★ 三类点打点对照（核心）**：文献靶心 + 示范音点 + 用户多遍点，同框于 F1–F2 图；展示用户点的中心与散布，并给每遍到靶心的距离。

> 即使真人示范音未就绪，凭文献靶心，功能 3/7/8 也应完整跑通；功能 4/5 待示范音补齐。

### 阶段二（增强）— 后续

- 实时音频分析（WebSocket 流式）。
- 辅音：先做**擦音频谱重心**（/s/ vs /ʃ/，center of gravity，可靠可量化）；VOT、共振峰过渡更靠后。
- 易混元音对比训练（/iː/-/ɪ/、/æ/-/ɛ/ 等）。
- 双元音共振峰轨迹图。
- 评分/方向反馈完善（舌位方向提示）。
- 宽带声谱图显示。
- IPA 图与 F1–F2 图联动高亮。
- 理论页交互滑块（拖动共振峰看输出变化）。

---

## 5. 数据规格

### 5.1 元音清单（12 个英音单元音）

| # | IPA | 例词 | 类型 |
|---|-----|------|------|
| 1 | iː | sheep | 长/前高 |
| 2 | ɪ | ship | 短/前次高 |
| 3 | e | bed | 短/前中 |
| 4 | æ | cat | 短/前低 |
| 5 | ɑː | car | 长/后低 |
| 6 | ɒ | hot | 短/后低 |
| 7 | ɔː | door | 长/后中 |
| 8 | ʊ | book | 短/后次高 |
| 9 | uː | food | 长/后高 |
| 10 | ʌ | cup | 短/央 |
| 11 | ɜː | bird | 长/央 |
| 12 | ə | about | 弱读央元音 |

### 5.2 参考共振峰数据格式

标准靶心采用**文献参考值**（英音 RP，如 Deterding 2004 等对标准南部英音的测量），存 `backend/app/data/vowels_rp.json`。

> **重要**：以权威语音学文献的英音 RP 共振峰测量值填充，**不要凭记忆编造数字**。下面是**结构**与占位示例，实际数值查证后填入，并在文件/README 标明出处与性别基准（共振峰有性别差异，须区分或注明）。

```json
{
  "meta": {
    "accent": "British English (RP)",
    "source": "填写文献出处",
    "gender_basis": "male / female / averaged",
    "unit": "Hz"
  },
  "vowels": [
    {
      "id": "iː",
      "ipa": "iː",
      "example_word": "sheep",
      "f1_mean": 0,
      "f2_mean": 0,
      "f1_sd": 0,
      "f2_sd": 0,
      "demo_audio": null
    }
  ]
}
```

- `f1_mean`/`f2_mean`：靶心中心；`f1_sd`/`f2_sd`：标准差，用于画可接受范围椭圆（±1 或 ±1.5 SD）。
- `demo_audio`：示范音路径，未就绪为 `null`。

### 5.3 示范音（后补）

- 优先找带授权的真人 RP 元音发音；TTS 可作临时替代但标注"非标准靶心"。
- 就绪后由服务端预分析缓存共振峰（功能 5）。缺失时前端占位，其余功能照常。

---

## 6. 后端分析与 API

### 6.1 `GET /api/vowels`
返回 12 元音参考数据（读 `vowels_rp.json`）；若示范音就绪，附带每个元音示范音的预分析 F1/F2（示范音点）。

### 6.2 `POST /api/analyze/formants`（核心，返回多段）
- 请求：multipart 一整段录音（可含多遍；浏览器 webm/opus，后端用 ffmpeg 转 wav）。可选 `target_vowel_id`。
- 处理流水线：**转码 → VAD 自动分遍 → 每遍内部取稳态段 → 逐遍提取 F1/F2/F3 与 F0 → 逐遍质量控制 → 汇总统计**。
- 响应示例：
```json
{
  "target_vowel_id": "iː",
  "takes": [
    {
      "index": 1,
      "f0": 0, "f1": 0, "f2": 0, "f3": 0,
      "start_ms": 0, "end_ms": 0, "duration_ms": 0,
      "quality": "ok",
      "distance_to_target": 0
    }
  ],
  "summary": {
    "valid_count": 0,
    "f1_center": 0, "f2_center": 0,
    "f1_spread": 0, "f2_spread": 0,
    "mean_distance_to_target": 0
  },
  "warnings": []
}
```
- `quality`：`ok` / `low_energy` / `too_short` / `formant_unreliable` 等；无效遍标记后不计入 `summary`，但仍返回供前端提示。
- `distance_to_target`、方向提示（如"舌位偏低/偏后"）可作为增强逐步完善。

### 6.3 自动分遍（VAD，segmentation.py）
- **原理**：短时能量（可用 parselmouth intensity 轮廓）找发声段，段间静音作分隔。
- **参数（全部可配置，§6.5）**：
  - 遍间静音阈值：连续静音 ≥ 约 **400 ms** 才算分遍（默认，实测调整），避免把一遍内的小停顿误切。
  - 最短有效时长：短于约 **100–150 ms** 的段丢弃（滤除点击声、呼吸声）。
  - 能量阈值：**自适应/相对**，先估背景噪声或取整体能量百分位，勿用绝对阈值（换设备失效）。
- **手动兜底**：自动切分结果明显异常（只切出 1 段但用户录了多遍、或碎段过多）时，允许用户手动调整或退回逐遍录音，避免卡死。

### 6.4 稳态段截取与取值（segmentation.py / analysis.py）
每一遍切出后，不用整遍，而是取稳态段（元音的"真身"，避开起始/收尾过渡）：
- **阶段一默认**：取该遍时长**中段窗口**（如中间 40–60%）→ 逐帧 F1/F2 → 取**中位数**（对追踪跳变稳健）。
- **可升级（若中段不准）**：在中部滑动小窗，选 F1/F2 逐帧方差最小（最平）的窗口取值。
- VAD 端点检测与"分遍""稳态截取"复用**同一套能量端点检测**：外层切每一遍，内层在每遍中取稳态段，一处实现两处用。

### 6.5 parselmouth 分析要点与质量控制
- 共振峰：Burg 法（Praat 默认），最大共振峰数 5，最大频率上限男声约 5000 Hz、女声约 5500 Hz（**可配置**）。
- 预处理：统一采样率、单声道。
- **共振峰合理性检查（quality.py）**：F1<F2<F3 且各自落在生理合理范围内；异常帧丢弃；整遍异常则标 `formant_unreliable`。这对女声/儿童及 F1/F2 靠近的元音（如 /ɜː/）尤为必要。
- **可外部化配置项汇总**：参考数据路径、共振峰频率上限、遍间静音阈值、最短有效时长、能量阈值策略、中段窗口比例、CORS 来源。

---

## 7. 理论介绍页规格（功能 1）

一页可视化科普，练习前建立"声源-滤波器"心智模型。

### 7.1 讲解结构
1. **声源**：声带周期开合把气流斩成脉冲 → "基频 F0 + 一系列谐波"（等间距、能量平滑下降的"谐波梳子"）。不同元音的声源几乎相同。
2. **滤波器**：声道是共鸣腔，放大靠近其共振频率的谐波、压制其余 → 形成共振峰 F1、F2。
3. **相乘**：谐波梳子 × 共振峰包络 = 听到的元音频谱，出现 F1、F2 隆起。
4. **区分元音**：改变舌唇颌 = 改变共鸣腔 = 移动 F1、F2。F1 关联舌位高低，F2 关联前后。
5. 呼应练习页：用户在 F1–F2 图上的点，就是这一步的测量结果。

### 7.2 核心配图：声源-滤波器三层图（SourceFilterFigure，SVG/Canvas）
三层纵向：
- 第一层：均匀下降的谐波梳子（声源），标 F0。
- 第二层：起伏的共振峰包络，标 F1、F2 两凸起。上下层间用 "×"。
- 第三层：相乘结果——F1/F2 下方谐波被顶高（高亮），其余压低（灰），标 F1、F2 隆起。用 "=" 连接。

### 7.3 可选增强（阶段二）
- 交互滑块：拖动第二层共振峰位置，实时看第三层输出变化，直观演示"移动共振峰 = 换元音"。

---

## 8. 前端交互要点

- **录音组件**：Web Audio API 采集；用户可自然地连录多遍；录后可回放再上传。处理麦克风权限与失败提示。
- **多遍结果面板（MultiTakePanel）**：列出每遍 F1/F2/时长/质量标记；展示整组中心、散布（标准差）、每遍到靶心距离；低质量遍提示"第 N 遍质量低，已忽略"并可手动剔除；提供自动分遍失败时的手动兜底入口。
- **F1–F2 图（FormantChart）**：严格反向坐标；渲染 12 靶心 + 椭圆、示范音点、用户多遍点（按遍编号/时序区分）+ 用户点的中心与散布椭圆；图例区分三类点。
- **IPA 四边形**：静态展示 12 元音标准位置，作舌位动作参考。
- **状态处理**：示范音缺失、分析失败、麦克风不可用、分遍异常等均有清晰 UI 反馈。
- 界面语言：中文；音标符号保留 IPA。

---

## 9. Docker 化（预留规格，后期实现）

阶段一**不写** Dockerfile，仅遵守 §2.3 可容器化约束；后期按下述规格一次性加上，列为后期里程碑。

### 9.1 后端 Dockerfile 规格
- 基于官方 Python 镜像；用 uv 安装依赖（容器内同样快）。
- **显式安装系统依赖 ffmpeg**（webm 转码），以及必要时的编译工具链，避免"本地能跑、容器缺库"。
- 复制 `pyproject.toml` + `uv.lock` 先装依赖再拷代码（利用缓存）。
- 入口 `uvicorn` 启动 FastAPI。

### 9.2 前端与整合
- 前端 `vite build` 产出静态 dist；**打包形态由 FastAPI 直接托管 dist**，单容器运行整个应用（部署最简）。
- 开发形态仍前后端分离（Vite dev server + FastAPI，CORS 放通本地）。

### 9.3 docker-compose 规格
- 即使单服务，也提供 compose 管理端口、卷挂载（示范音、参考数据、用户录音临时目录）、环境变量（§6.5 配置项）。
- 配置全部经环境变量注入，同一镜像多环境可用。

---

## 10. 里程碑

**M1 — 后端骨架**：uv 环境 + FastAPI + parselmouth 打通；`/api/vowels` 返回参考数据。

**M2 — 分遍与稳态分析（阶段一核心难点）**：ffmpeg 转码 + VAD 自动分遍 + 每遍稳态取值 + 质量控制；`/api/analyze/formants` 对含多遍的录音返回多段数组 + 汇总统计。重点保证 VAD 可靠，参数按 §6.3/6.4 可配置，含手动兜底。

**M3 — 前端两张图**：IPA 四边形 + F1–F2 图（正确反向坐标）+ 12 靶心与椭圆。

**M4 — 录音与三类点对照（核心）**：浏览器多遍录音 → 上传 → 多段分析 → 用户多遍点 + 中心/散布 打入 F1–F2 图，与文献靶心对照；多遍结果面板。

**M5 — 理论介绍页**：声源-滤波器讲解 + 三层图组件。

**M6 — 示范音接入 + 打磨**：示范音就绪后服务端预分析缓存，作为示范音点接入（三类点集齐）；方向提示、UI 完善。

**后期里程碑**：Docker 化（§9）；阶段二增强（实时 WebSocket、擦音频谱重心、易混元音训练、双元音轨迹、声谱图、图联动）。

---

## 11. 给 Claude Code 的开发注意事项

1. **后端不直接访问麦克风**；录音一律走浏览器 Web Audio API，音频上传后端分析。
2. **参考共振峰数值必须查证权威英音 RP 文献后填入 `vowels_rp.json`，不得凭记忆编造**；标注出处与性别基准。
3. **F1–F2 图坐标轴务必反向**（F1 向下增、F2 向左增），与 IPA 四边形形状对应——教学价值关键，勿用常规正向坐标。
4. **自动分遍（VAD）是阶段一关键路径**，须调到可靠：遍间静音阈值（~400ms）、最短时长门槛（~100–150ms）、自适应能量阈值，全部可配置，并提供手动兜底。
5. **VAD 端点检测与稳态截取复用同一套能量端点检测**：外层分遍、内层取稳态，勿重复实现。
6. **稳态取值**：每遍取中段窗口 → 逐帧 F1/F2 取中位数；必要时升级为"最平窗口"筛选。
7. **逐遍质量控制**：无效遍（太短/能量低/共振峰不合理）标记并排除出统计，但仍返回供前端提示；含 F1<F2<F3 及生理范围合理性检查。
8. 后端环境用 **uv**（pyproject.toml + uv.lock）；WSL 上注意 parselmouth 可能需编译工具链、webm 需 ffmpeg 转码，README 写明。
9. **遵守可容器化约束**（§2.3）：系统依赖清单化、配置外部化、前端可被后端托管；但阶段一不写 Dockerfile，Docker 按 §9 规格后期加。
10. 阶段一即使无示范音也要能完整运行"自动分遍 + 三类点中的两类（靶心+用户）对照"；示范音相关 UI 做好缺省处理。
11. 前端 React + Vite + TypeScript；界面中文，音标保留 IPA。
12. 优先保证阶段一闭环可用，再做阶段二；勿过早引入实时分析复杂度。
