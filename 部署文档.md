# 部署文档 — 英音发音辅助学习系统

面向本项目的三种部署形态，从开发到生产逐层递进。所有形态都遵守
「配置外部化、前端可被后端托管、系统依赖清单化」（开发文档 §2.3）。

| 形态 | 场景 | 前端 | 后端 | 备注 |
|---|---|---|---|---|
| **A. 本地开发** | 日常开发、改代码热更新 | Vite dev server (:5173) | uvicorn --reload (:8000) | 前后端分离，CORS 放通本地 |
| **B. 单机生产（无 Docker）** | 一台 Linux/WSL 主机直接跑 | `vite build` 产出 dist，由 FastAPI 托管 | uvicorn（多 worker）+ systemd | 单进程/单端口，可选 Nginx 反代 |
| **C. Docker 单容器** | 可移植、一条命令起停 | 构建期打进镜像，FastAPI 托管 | 容器内 uvicorn (:8000) | 内置 ffmpeg，`docker compose up` |

> **关键约束**：录音一律由**浏览器**采集（Web Audio API），后端从不访问麦克风。
> 因此浏览器需通过 **HTTPS 或 localhost** 访问才能拿到麦克风权限（见 §5）。

---

## 0. 前置系统依赖

| 依赖 | 形态 A/B 需要 | 形态 C 需要 | 安装（Debian/Ubuntu/WSL） |
|---|:--:|:--:|---|
| Python ≥ 3.11 | ✅ | 镜像内置 | 系统自带 / `uv python install 3.12` |
| [uv](https://docs.astral.sh/uv/) | ✅ | 镜像内置 | `curl -LsSf https://astral.sh/uv/install.sh \| sh` |
| **ffmpeg** | ✅ | 镜像内置 | `sudo apt install ffmpeg` |
| Node ≥ 18 + npm | ✅ | 镜像内置 | `sudo apt install nodejs npm` 或 nvm |
| Docker + Compose | ❌ | ✅ | 参考 Docker 官方文档 |

> ffmpeg 用于把浏览器录音（webm/opus）转成 parselmouth 可读的 WAV。
> **缺失时**后端仍能分析直接上传的 WAV，但浏览器录音会返回明确错误。
> 用 `GET /api/health` 的 `ffmpeg_available` 字段自检。

---

## 形态 A：本地开发（前后端分离）

```bash
# 后端
cd backend
uv sync
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 前端（另开一个终端）
cd frontend
npm install
npm run dev            # http://localhost:5173
```

- 前端默认通过 `VITE_API_BASE`（缺省 `http://localhost:8000`）访问后端。
- 需要改后端地址时，在 `frontend/.env.local` 写 `VITE_API_BASE=http://<host>:8000`。
- 后端 CORS 默认放通 `http://localhost:5173`、`http://127.0.0.1:5173`；
  换端口/域名时改配置项 `cors.allow_origins`（见 §4）。

---

## 形态 B：单机生产（FastAPI 托管前端，无 Docker）

一台主机、一个端口即可对外提供整个应用。

### B.1 构建前端

```bash
cd frontend
npm install
# 同源部署：API 基址留空，前端用相对路径 /api/... 调后端
VITE_API_BASE="" npm run build      # 产出 frontend/dist
```

### B.2 让后端托管 dist 并启动

```bash
cd backend
uv sync
export VOWEL_TRAINER_GENERAL_FRONTEND_DIST="$(cd ../frontend/dist && pwd)"
export VOWEL_TRAINER_CORS_ALLOW_ORIGINS=""     # 同源无需 CORS
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
```

访问 `http://<主机IP>:8000/`：SPA 由 `/` 提供，API 在 `/api/*`，**同源**无 CORS 问题。
`GET /api/health` 的 `frontend_served` 应为 `true`。

> **worker 数**：parselmouth 分析是 CPU 密集且占内存，建议 `--workers = CPU 核数`
> 的一半到一倍，按内存实测调整。

### B.3 用 systemd 常驻（推荐）

新建 `/etc/systemd/system/vowel-trainer.service`（把路径/用户改成你的）：

```ini
[Unit]
Description=English Vowel Trainer (FastAPI + parselmouth)
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/home/youruser/project/phonetic_symbol_visualization/backend
Environment=VOWEL_TRAINER_GENERAL_FRONTEND_DIST=/home/youruser/project/phonetic_symbol_visualization/frontend/dist
Environment=VOWEL_TRAINER_CORS_ALLOW_ORIGINS=
ExecStart=/home/youruser/.local/bin/uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now vowel-trainer
sudo systemctl status vowel-trainer
journalctl -u vowel-trainer -f            # 看日志
```

> `uv` 的绝对路径用 `which uv` 查得（常见为 `~/.local/bin/uv`）。

### B.4 （可选）Nginx 反向代理 + HTTPS

麦克风权限要求 HTTPS（localhost 除外）。用 Nginx 终止 TLS 并反代到 :8000：

```nginx
server {
    listen 443 ssl;
    server_name your.domain;

    ssl_certificate     /etc/letsencrypt/live/your.domain/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your.domain/privkey.pem;

    client_max_body_size 25m;         # 录音上传，放宽一点

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

证书用 `certbot --nginx` 一键申请。之后从 `https://your.domain` 访问即可录音。

---

## 形态 C：Docker 单容器

镜像内已装 **ffmpeg**，并把前端 dist 打进镜像由 FastAPI 托管，一个容器跑全栈。

### C.1 一条命令起停（compose）

```bash
# 仓库根目录
docker compose -f docker/docker-compose.yml up --build -d
docker compose -f docker/docker-compose.yml logs -f
docker compose -f docker/docker-compose.yml down
```

访问 `http://<主机IP>:8000/`。

### C.2 或直接用 docker build/run

```bash
# 构建上下文是仓库根目录
docker build -f docker/Dockerfile -t english-vowel-trainer:latest .

docker run -d --name vowel-trainer -p 8000:8000 \
  -e VOWEL_TRAINER_GENERAL_DEFAULT_GENDER=male \
  english-vowel-trainer:latest
```

### C.3 镜像结构（多阶段）

1. **frontend 阶段**（`node:22-slim`）：`npm ci` + `npm run build`，
   构建期 `VITE_API_BASE=""` 使前端走同源相对路径。
2. **runtime 阶段**（`python:3.12-slim`）：`apt install ffmpeg` →
   用 uv 按 `uv.lock` 装依赖（先拷 lock 再拷代码，利用缓存）→
   拷入 app 代码与前端 dist → `uvicorn app.main:app`。

### C.4 挂载与配置

- 示范音免重建镜像更新：compose 已挂载
  `backend/app/data/demo_audio → /app/backend/app/data/demo_audio:ro`。
- 所有参数经环境变量注入（见 §4），同一镜像多环境复用。
- 健康检查：compose 内置 `/api/health` 探针。

---

## 4. 配置项参考（环境变量）

配置优先级：**环境变量 `VOWEL_TRAINER_<SECTION>_<KEY>` > `backend/config.toml` > 内置默认**。
完整默认值见 `backend/config.example.toml`。常用项：

| 环境变量 | 含义 | 默认 |
|---|---|---|
| `VOWEL_TRAINER_GENERAL_DEFAULT_GENDER` | 默认参考基准 male/female | `male` |
| `VOWEL_TRAINER_GENERAL_FRONTEND_DIST` | 要托管的前端 dist 绝对路径（留空=不托管） | 空 |
| `VOWEL_TRAINER_GENERAL_REFERENCE_DATA_PATH` | 参考数据文件路径 | `app/data/vowels_rp.json` |
| `VOWEL_TRAINER_GENERAL_DEMO_AUDIO_DIR` | 示范音目录 | `app/data/demo_audio` |
| `VOWEL_TRAINER_CORS_ALLOW_ORIGINS` | 允许的前端来源（逗号分隔；同源留空） | 本地 5173 |
| `VOWEL_TRAINER_FORMANTS_MAX_FREQUENCY_MALE` | 男声共振峰频率上限 Hz | `5000` |
| `VOWEL_TRAINER_FORMANTS_MAX_FREQUENCY_FEMALE` | 女声共振峰频率上限 Hz | `5500` |
| `VOWEL_TRAINER_SEGMENTATION_SILENCE_SPLIT_MS` | 遍间静音阈值 ms | `400` |
| `VOWEL_TRAINER_SEGMENTATION_MIN_TAKE_MS` | 最短有效遍时长 ms | `120` |
| `VOWEL_TRAINER_SEGMENTATION_THRESHOLD_DB_ABOVE_FLOOR` | 能量阈值（噪声底之上 dB） | `8` |
| `VOWEL_TRAINER_STEADY_STATE_MID_WINDOW_RATIO` | 稳态取中段比例 | `0.5` |
| `VOWEL_TRAINER_QUALITY_MIN_VALID_FRAME_RATIO` | 判定有效遍的最小合格帧比例 | `0.5` |

> `config.toml` 里也可用同样的分节结构覆盖（`cp config.example.toml config.toml` 后编辑）。
> 环境变量始终优先，适合容器/多环境。

---

## 5. 验证与排错

### 5.1 冒烟验证

```bash
curl -s http://<host>:8000/api/health            # status/ffmpeg_available/frontend_served
curl -s "http://<host>:8000/api/vowels?gender=male" | head
# 上传一段 WAV 走完整分析（无需 ffmpeg 即可测这条）
curl -s -F "file=@some.wav;type=audio/wav" -F "target_vowel_id=iː" -F "gender=male" \
     http://<host>:8000/api/analyze/formants
```

后端自带合成音冒烟测试：

```bash
cd backend && PYTHONPATH=. uv run python tests/smoke_pipeline.py
```

### 5.2 常见问题

| 现象 | 原因 / 处理 |
|---|---|
| 页面能打开但点录音无反应 | 麦克风需 **HTTPS 或 localhost**；用 Nginx+证书（§B.4）或本机访问 |
| `health.ffmpeg_available=false` 且录音分析报错 | `sudo apt install ffmpeg`（形态 A/B）；Docker 已内置 |
| 前端报「无法连接后端」 | `VITE_API_BASE` 指错，或 CORS 未放通该来源（改 `cors.allow_origins`） |
| `frontend_served=false` 但想同源托管 | 没设 `VOWEL_TRAINER_GENERAL_FRONTEND_DIST` 或路径不对 |
| 自动分遍只切出 1 段/切碎 | 调 `SILENCE_SPLIT_MS` / `MIN_TAKE_MS` / `THRESHOLD_DB_ABOVE_FLOOR`；前端也提供手动剔除兜底 |
| 上传大录音 413 | Nginx 调 `client_max_body_size` |
| parselmouth 安装需编译 | `sudo apt install build-essential`（Linux 一般有预编译 wheel，无需） |

---

## 6. 升级与回滚

```bash
# 形态 B
git pull
cd frontend && VITE_API_BASE="" npm run build
cd ../backend && uv sync
sudo systemctl restart vowel-trainer

# 形态 C
git pull
docker compose -f docker/docker-compose.yml up --build -d   # 重建并滚动重启
# 回滚：git checkout <上一个 tag/commit> 后重建，或保留旧镜像 tag 再 run
```

> 依赖版本由 `backend/uv.lock` 与 `frontend/package-lock.json` 锁定，保证可复现。
